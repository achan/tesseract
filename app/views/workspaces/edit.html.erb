<% content_for(:title, "Edit Workspace — #{@workspace.team_name || @workspace.team_id}") %>

<div class="form-container">
  <h2>Edit Workspace</h2>
  <%= render "form", workspace: @workspace %>
</div>
