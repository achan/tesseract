<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Slack Summary" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-background text-foreground antialiased">
    <div class="grid grid-cols-[240px_1fr] min-h-screen">
      <aside class="bg-sidebar text-sidebar-foreground p-4 overflow-y-auto">
        <h1 class="text-lg text-white mb-6 pb-3 border-b border-white/20">
          <%= link_to "Slack Summary", root_path, class: "text-inherit no-underline" %>
        </h1>
        <nav>
          <% Workspace.includes(:slack_channels).find_each do |workspace| %>
            <div class="mb-6">
              <h2 class="text-xs uppercase tracking-wide text-sidebar-muted mb-2"><%= workspace.team_name %></h2>
              <ul class="space-y-0.5">
                <% workspace.slack_channels.active.channels.order(:channel_name).each do |channel| %>
                  <li>
                    <%= link_to "##{channel.channel_name || channel.channel_id}",
                          workspace_slack_channel_path(workspace, channel),
                          class: "block px-2 py-1 text-sm text-sidebar-foreground no-underline rounded-sm hover:bg-sidebar-accent hover:text-white" %>
                  </li>
                <% end %>
              </ul>
            </div>
          <% end %>
        </nav>
      </aside>

      <main class="p-8 overflow-y-auto">
        <% if notice.present? %>
          <div class="bg-green-50 border border-success rounded-md px-4 py-3 mb-6 text-green-800 text-sm"><%= notice %></div>
        <% end %>
        <%= yield %>
      </main>
    </div>
  </body>
</html>
